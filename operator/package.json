{
  "name": "@veris/operator",
  "version": "1.0.0",
  "description": "Veris off-chain operator/signing service — fetches data, signs freshness attestations, resolves ERC-8183 jobs on Monad testnet",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node --esm src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ethers": "^6.13.4",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.4",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
